// Single source of truth lives in src/constants/theme.ts.
// This file re-exports everything so screens can import from either path.
export {
  Colors,
  Spacing,
  BorderRadius,
  Typography,
  Shadow,
} from '@src/constants/theme';
